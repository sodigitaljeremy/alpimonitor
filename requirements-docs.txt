# AlpiMonitor documentation site (docs.alpimonitor.fr)
# Pinned for reproducibility; bump versions intentionally.
#
# Local preview:   pip install -r requirements-docs.txt && mkdocs serve
# Static build:    mkdocs build -d site/
# Production:      served by a dedicated nginx container (see Phase 4).

mkdocs==1.6.1
mkdocs-material==9.5.44
mkdocs-mermaid2-plugin==1.2.1
mkdocs-kroki-plugin==0.9.0
mkdocs-awesome-pages-plugin==2.9.3

# Transitive but listed for pip-compile style visibility.
pymdown-extensions==10.11.2
