/**
 * Initial Leaflet viewport: centered on the Rhône valaisan at a zoom
 * level that keeps both the main Rhône corridor and the Val d'Hérens
 * markers in view without scrolling.
 */
export const MAP_CENTER: [number, number] = [46.22, 7.4];
export const MAP_ZOOM = 10;

/** Tile layer max zoom — matches the OSM standard tile pyramid. */
export const MAX_ZOOM = 18;
